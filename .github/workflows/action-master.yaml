name: Echo Message

on: 
  push:
    branches:
      - master

jobs:
  print_message:
    runs-on: ubuntu-latest
    steps:
      - name: Print Message
        run: echo "Hello, world! This is a GitHub Actions workflow echo message."
